export interface IItem {
    id: string;
    title: string;
}
export interface ITabItem {
    id: string;
    title: string;
}

export interface IRepHeader {
    title: string;
    date: any;
    numberOfAccounts: number;
}